apiVersion: v1
kind: Service
metadata:
  labels:
    app: FunFacts
    role: web
  name: con-web
spec:
  ports:
    - name: "9090"
      port: 9090
      targetPort: 5000
  selector:
    app: FunFacts
    role: web
  type: NodePort
