apiVersion: v1
kind: Service
metadata:
  labels:
    app: FunFacts
    role: web
  name: con-web
spec:
  ports:
    - name: "9080"
      port: 9080
      targetPort: 5000
  selector:
    app: FunFacts
    role: web
  type: NodePort
